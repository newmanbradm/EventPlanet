import React from 'react';

const EventTab = (props) => {

    return (
        <div className="event-tab">
           <h5>{props.title}</h5> 
           <hr></hr>
        </div>
    );
}

export default EventTab;
